/**
 * Minimal ESLint flat config for Atlas Doctor custom product feature roots.
 *
 * Used only by Atlas Doctor programmatic linting — not by `pnpm lint`.
 */
export default [
  {
    languageOptions: {
      ecmaVersion: 2022,
      sourceType: "module",
    },
  },
];
