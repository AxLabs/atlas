# Reference components

UI compositions in this directory demonstrate how to wire Atlas platform infrastructure into
product-facing components. They are **not mounted** in the current reference application and are
**safe to delete or replace**.

Do not treat these as platform primitives. Copy or adapt them when building your product shell
(#39).
