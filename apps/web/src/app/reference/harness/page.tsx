import { ReferenceHarnessPage } from "@/features/reference";

export const metadata = {
  title: "Reference harness",
  description: "Deterministic persona and API scenario controls for local development",
};

export default function ReferenceHarnessRoute() {
  return <ReferenceHarnessPage />;
}
