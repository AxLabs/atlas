import { ReferenceOverview } from "@/features/reference";

export const metadata = {
  title: "Reference application",
  description: "Coherent Atlas consumer journey with deterministic local auth and API fixtures",
};

export default function ReferencePage() {
  return <ReferenceOverview />;
}
